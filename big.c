#include <stdio.h>

int main(){
    // int a,b,c;
    // scanf("%d %d %d", &a,&b,&c);
    // int max_value = a;
    // if(b > max_value) max_value = b;
    // if(c > max_value) max_value = c;
    // printf("%d eh o maior\n", max_value);

    printf("Hello\n");
    return 0;
}